1. In data folder, sents.train is the training dataset, sents.test is the test dataset without tags, sents.answer is the correct tag of test dataset, sents.output_hmm is the result of test.

2. In hmm folder, hmm_add_k_smooth.py contains the training process and test process. hmm_add_k_smooth, evalution.py and statistics.py can run directly. hmm_viterbi.ipynb can be implemented in Google Colab